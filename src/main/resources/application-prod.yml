# 운영 환경 전용 설정
app:
  cookie-secure: true

# 운영에서 Swagger UI 비활성화
springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false

logging:
  level:
    root: WARN
    org.dallyeo.matuabom: INFO
    org.springframework.security: WARN
